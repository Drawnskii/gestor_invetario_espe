import 'package:flutter/material.dart';

class ObjectRegistry extends StatelessWidget {
  const ObjectRegistry({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Formulario para registrar un objeto'),
      ),
    );
  }
}